FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY roborock-bridge/pyproject.toml ./
RUN pip install --no-cache-dir .

# Copy source code
COPY roborock-bridge/ ./

EXPOSE 3002

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "3002"]
